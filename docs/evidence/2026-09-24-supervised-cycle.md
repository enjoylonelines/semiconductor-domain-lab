# Supervised P0 cycle evidence: execution-state false success and STA fixture gate

Date: 2026-09-24  
Repository: `/Users/hb/Projects/semiconductor-domain-lab`  
Base commit: `bb4997bfa46ca63d112866b029ceca3569fdfba8` (`bb4997b`)

## Scope and environment

This is one isolated synthetic parser/service experiment using temporary report files and an in-memory SQLite `Store`. It made no production write, external request, EDA tool invocation, hardware call, new infrastructure, or parser-format change. Human hypothesis and Human Prediction were `unrecorded`; interpretation, adoption/rejection, and Changed belief are pending human decision.

## Baseline reproduction

Regression fixture in `tests/test_stage1.py::Stage1Tests.test_service_does_not_persist_nonzero_tool_exit_as_succeeded`:

- synthetic marker/version, `tool_exit_code=1`, and parseable `worst_slack=0.12 ns`;
- test double returns that fixed temporary artifact to `JobService`;
- expected invariant: run and attempt are not `SUCCEEDED`, while parse/check/raw exit values remain observable.

Before the mapping change, command:

```sh
PYTHONPATH=src python3 -B -m unittest tests.test_stage1.Stage1Tests.test_service_does_not_persist_nonzero_tool_exit_as_succeeded -v
```

Outcome: `FAIL` (1 test, 0.005s): persisted run status was `SUCCEEDED`, not expected `FAILED`. This matches the earlier synthetic observation in `docs/evidence/stage2-validation-matrix.md`; it is not real EDA execution evidence.

## Minimal reinforcement and identical-fixture revalidation

`src/eda_lab/service.py` now maps a parseable artifact whose `metrics.tool_exit_code != 0` to:

- attempt `FAILED`, `error_type=tool_exit`;
- run `FAILED`, preserving parser `parse_status`, timing `check_status`, completeness, provenance, artifact path, error, and raw metrics (including `tool_exit_code`).

No parser rule, report field, retry behavior, async lifecycle, UI, scheduler, or adapter contract was changed.

The exact same focused command passed: `OK` (1 test, 0.002s).

Relevant suite command:

```sh
PYTHONPATH=src python3 -B -m unittest discover -s tests -v
```

Outcome: `OK`; 18 tests passed in 0.477s. The existing negative-slack case continues to be `status=SUCCEEDED`, `parse_status=OK`, `check_status=FAIL`, preserving the execution-versus-check distinction.

## STA report fixture provenance and independent oracle inspection

Checkout inspection used:

```sh
find . -type f \( -iname '*sta*' -o -iname '*timing*' -o -iname '*report*' -o -iname '*.rpt' \) -not -path './.git/*' | sort
rg -n -i "tool_exit_code|source_kind|oracle|authentic|real report|fixture" . -g '!uv.lock' -g '!*.pyc'
```

No report artifact (`*.rpt` or traceable authentic STA output) is present. The parser accepts only an explicitly marked `SYNTHETIC / NOT GENERATED BY AN EDA TOOL` format and assigns `source_kind=synthetic`; `SyntheticTimingAdapter` states that it never represents real tool output. The existing `Real*Adapter` tests inject fake clients and do not produce an STA report. `docs/references/source-register-2026-09-23.md` also records no confirmed available STA tool/output in its stated inspection scope.

There is no independent raw-field oracle in this checkout: the only raw-field extraction is `src/eda_lab/parser.py`, and current test assertions read that parser result. Therefore no synthetic/reference fixture is promoted to human gold or authentic-report evidence.

### Actual STA fixture acceptance gate

Stop at this gate until a human authorizes/provides one traceable public or otherwise authorized actual STA report together with its provenance. Acceptance requires, at minimum:

1. immutable raw report file and SHA-256;
2. source URL/origin, license/redistribution status, tool/flow/version, command or reproducible generation path, design/corner/constraints/path scope, and reported units;
3. an oracle recorded independently of parser code (manual source-line extraction plus tool-native result, where available) for the supported required fields;
4. explicit field-by-field comparison of raw/oracle values to normalized values, with synthetic derivatives separately labeled; and
5. only then, a narrowly scoped parser acceptance experiment for that existing format.

This task did not obtain, fabricate, alter, or parse any such report.

## Limits and stop condition

- The successful revalidation proves only this service-state mapping on one isolated synthetic fixture. It does not prove actual EDA exit propagation, report-format correctness, parser completeness, finite-number handling, retry semantics for tool exit, HTTP behavior, or production durability.
- The independent oracle and authentic-report portions of P0 remain unmet.
- Budget was exhausted: one baseline, one minimal mapping change, identical-fixture revalidation, and provenance/oracle inspection. STOP at the pending human decision.
